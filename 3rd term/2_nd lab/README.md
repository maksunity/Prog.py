# Лабораторная работа №2: Веб-скрапинг с Scrapy и Playwright

## Описание

Система сбора структурированных данных с современных веб-сайтов, поддерживающая как классические статические сайты (SSR), так и динамические SPA-приложения с JavaScript-рендерингом.

## Функциональность

- 🕷️ **Scrapy Spider** для статических сайтов (SSR)
- 🎭 **Playwright интеграция** для динамического контента
- 📰 **News Spider** - сбор новостей с различных порталов
- 🛒 **E-commerce Spider** - парсинг товаров из интернет-магазинов
- 🔄 **Автоматические повторы** при ошибках
- 💾 **Экспорт данных** в JSON и CSV форматах

## Использование

### News Spider - сбор новостей

```bash
# Сбор всех новостей
python run_spider.py --spider news --output news.json

# Поиск новостей по ключевым словам
python run_spider.py --spider news --keywords "ПНИПУ" --days 7 --output news_pstu.json

# Ограничение по времени
python run_spider.py --spider news --keywords "технологии" --days 30 --output tech_news.json
```

### E-commerce Spider - парсинг товаров

```bash
# Сбор товаров по категории
python run_spider.py --spider ecommerce --category "ноутбуки" --output laptops.json

# Мультимагазинный поиск
python run_spider.py --spider ecommerce --category "смартфоны" --stores "wildberries,ozon" --output phones.csv

# Экспорт в CSV
python run_spider.py --spider ecommerce --category "наушники" --output headphones.csv
```

## Примеры собранных данных

### Новостная статья (news.json)

```json
{
  "title": "Студенты ПНИПУ победили в конкурсе",
  "url": "https://pstu.ru/news/article/...",
  "date": "2025-01-15",
  "source": "ПНИПУ",
  "summary": "Команда студентов Пермского Политеха...",
  "category": "Образование",
  "keywords": ["ПНИПУ", "конкурс", "студенты"]
}
```

### News Spider

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `keywords` | Ключевые слова для фильтрации | None |
| `days` | Период сбора новостей (дни) | 30 |
| `max_pages` | Максимум страниц для обхода | 10 |

### E-commerce Spider

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `category` | Категория товаров | None |
| `stores` | Список магазинов через запятую | "wildberries" |
| `min_rating` | Минимальный рейтинг | 4.0 |
| `max_price` | Максимальная цена | None |

## Возможности Scrapy-Playwright

### Обход анти-бот систем

```python
# Эмуляция реального пользователя
PLAYWRIGHT_BROWSER_TYPE = 'chromium'
PLAYWRIGHT_LAUNCH_OPTIONS = {
    'headless': True,
    'args': ['--disable-blink-features=AutomationControlled']
}
```

### Ожидание JavaScript

```python
# Ожидание загрузки динамического контента
await page.wait_for_selector('.product-list')
await page.wait_for_load_state('networkidle')
```
## Результаты работы

| Датасет | Записей | Формат | Размер |
|---------|---------|--------|--------|
| news.json | 68 | JSON | 156 KB |
| pstu.json | 52 | JSON | 98 KB |
| news_tech.json | 124 | JSON | 287 KB |
